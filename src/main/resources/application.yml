server:
  port: 8080
  servlet:
    contextPath: /api

spring:
  jpa:
    hibernate:
      ddl-auto: update

  datasource:
    url: jdbc:mysql://localhost:3306/db_webservice
    username: root
    password: 140841

  security:
    user:
      name: root
      password: 140841